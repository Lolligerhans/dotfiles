# nvim/lua/functions/

This folder is not from LazyVim. We put Lua scripts here that we want to load
with `require` (e.g. from a user-command in `nvim/plugin/commands.lua`).

When a function is directly plugin-related, better place it into a directory
next to the plugin spec `nvim/lua/plugins/*/`.
